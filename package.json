{
  "name": "rummi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client && node -e \"require('fs').cpSync('client/dist', 'dist', {recursive: true})\""
  }
}
